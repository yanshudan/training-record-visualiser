- 4 pages
# Start your plan today
- generate today's target plan on basis of past training history
- press start to start next set, show how much time you spend on this set and how much time you take for rest between sets
- if the target number of reps not met, allow user to slide
# configure your plan
- user should be able to set excecise, sets and reps, weights
- set how the want to progressive overload, like +1 rep next time, and +2.5kg if hits 10 rep last time (basically a group of rules)
- allow user to customize the excercises
- one problem is how to add new excercise easily, we were using plain text based solution now, but seems we need some UI in StartPlan page so user can add new excercise in place instead of going to ConfigurPlan page.
# Daily time table
- a time table defining daily schedule
- visualize that like a clock or flow? should be swithcable
# visualize the training record
- the vusualization should be based on user customized excercise definition, and visualize the past training data per body part
- a daily heatmap at the top shows actual training frequency, then the detailed charts below

# Improvements
- Optimise the charts (Stats page): shared theming/axes, memoised series, responsive sizing, lighter recharts usage / consider fewer chart types.
