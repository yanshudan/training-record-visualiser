# Container that holds all users' data (one blob prefix per user).
AZURE_STORAGE_ACCOUNT=
AZURE_STORAGE_KEY=
AZURE_STORAGE_CONTAINER=training-records

# Port the API listens on.
PORT=7071

# Comma-separated list of allowed web origins for CORS.
ALLOWED_ORIGINS=http://localhost:3123
